
  # Recreate design with hover effects

  This is a code bundle for Recreate design with hover effects. The original project is available at https://www.figma.com/design/DyToCjeQhoe4X4oU3Betje/Recreate-design-with-hover-effects.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  